# Будущие улучшения NeoxiderTools

## 🎯 Центральная система сохранений

### Проблема
В настоящее время различные компоненты используют `PlayerPrefs` напрямую, что создает:
- Отсутствие единой точки управления сохранениями
- Сложность перехода на альтернативные системы сохранения (JSON, Binary, Cloud)
- Невозможность централизованной валидации и шифрования данных
- Сложность тестирования и мокирования

### Решение: ISaveProvider

#### Архитектура
```
ISaveProvider (интерфейс)
    ├── PlayerPrefsSaveProvider (реализация через PlayerPrefs)
    ├── JsonSaveProvider (реализация через JSON файлы)
    ├── BinarySaveProvider (реализация через бинарные файлы)
    └── EncryptedSaveProvider (обертка с шифрованием)
```

#### Примерный план реализации

**Этап 1: Создание интерфейса и базовой реализации**
- [ ] Создать `ISaveProvider` интерфейс
- [ ] Реализовать `PlayerPrefsSaveProvider`
- [ ] Создать `SaveProviderManager` (Singleton) для управления провайдерами

**Этап 2: Рефакторинг существующих компонентов**
- [ ] Обновить `Money.cs` для использования `ISaveProvider`
- [ ] Обновить `ScoreManager.cs`
- [ ] Обновить `TimeReward.cs`
- [ ] Обновить `Collection.cs` и `Box.cs`
- [ ] Обновить `Map.cs`

**Этап 3: Расширенные возможности**
- [ ] Добавить валидацию данных при сохранении
- [ ] Добавить версионирование сохранений
- [ ] Реализовать автоматическое резервное копирование
- [ ] Добавить поддержку шифрования чувствительных данных

**Этап 4: Альтернативные провайдеры**
- [ ] Реализовать `JsonSaveProvider`
- [ ] Реализовать `BinarySaveProvider`
- [ ] Реализовать `EncryptedSaveProvider` (обертка)

**Этап 5: Интеграция с SaveManager**
- [ ] Интегрировать с существующим `SaveManager.cs`
- [ ] Обеспечить обратную совместимость
- [ ] Добавить миграцию данных из PlayerPrefs

### Пример использования

```csharp
// В компоненте
public class Money : Singleton<Money>
{
    private ISaveProvider _saveProvider;
    
    private void Awake()
    {
        _saveProvider = SaveProviderManager.Instance.GetProvider();
    }
    
    private void Load()
    {
        _money = _saveProvider.GetFloat("Money", 0);
    }
    
    private void Save()
    {
        _saveProvider.SetFloat("Money", _money);
    }
}
```

### Преимущества
- ✅ Единая точка управления сохранениями
- ✅ Легкая замена системы сохранения
- ✅ Возможность тестирования с моками
- ✅ Централизованная валидация и обработка ошибок
- ✅ Поддержка различных форматов сохранения

---

## 📋 Другие будущие улучшения

### Производительность
- Оптимизация аллокаций в часто вызываемых методах
- Кэширование результатов дорогих операций
- Использование object pooling для временных объектов

### Архитектура
- Разделение больших классов (Drawer, DialogueManager)
- Добавление интерфейсов для улучшения тестируемости
- Рефакторинг дублирующегося кода

### Качество кода
- Расширение XML-документации
- Добавление unit-тестов для критических компонентов
- Улучшение валидации в OnValidate


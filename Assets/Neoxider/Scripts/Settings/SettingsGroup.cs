namespace Neo.Settings
{
    /// <summary>Logical settings group for persist flags and reset.</summary>
    public enum SettingsGroup
    {
        Graphics = 0,
        Input = 1,
        Display = 2,
        Performance = 3
    }
}
